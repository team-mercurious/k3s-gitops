apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: api-generation
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: api-generation
  policy:
    alphabetical:
      order: desc
  filterTags:
    pattern: '^sha-[a-f0-9]+$'
    extract: '$0'