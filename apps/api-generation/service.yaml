apiVersion: v1
kind: Service
metadata:
  name: api-generation
  namespace: app
  labels:
    app: api-generation
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: api-generation